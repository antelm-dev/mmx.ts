---
"@mmx/runtime": minor
"@mmx/engine": minor
---

Add `@mmx/runtime/debug` with a UI-independent `DebugController` (time control, checkpoints, seek, cheats, replay I/O, diagnostics). Expose replay/taint APIs on engine `ToolingSession` and runtime sessions so Web and Studio share one debug host.
