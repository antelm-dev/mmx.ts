---
"@mmx/runtime": minor
"@mmx/engine": minor
"@mmx/editor-runtime": minor
---

Add `@mmx/runtime/debug` with a UI-independent `DebugController` (time control, checkpoints, seek, cheats, replay I/O, diagnostics). Expose recorder APIs on engine `ToolingSession` and runtime sessions. Wire Web and Studio playtest through the shared controller.
