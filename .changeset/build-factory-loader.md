---
"@mmx/build-tools": minor
---

Add `@mmx/build-tools` with a validated Studio export loader, content-hashed asset emission, the `virtual:mmx-project` Vite plugin, and `mmx-build` CLI. The web app composes injected project levels and asset resolvers when `MMX_PROJECT` is set.
