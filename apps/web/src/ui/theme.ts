/**
 * Shared palette for the player-facing menu screens (home + settings).
 *
 * Both screens draw on the same dark-panel/amber-highlight look, so the colors
 * live here once rather than as two independently-maintained copies that drift
 * the moment one screen's palette gets tweaked and the other doesn't.
 */
export const COLOR_BG = 0x050a16;
export const COLOR_SCRIM = 0x05070f;
export const COLOR_PANEL = 0x0b1622;
export const COLOR_BORDER = 0x395564;
export const COLOR_TEXT = 0xd7edf7;
export const COLOR_DIM = 0x7f9daa;
export const COLOR_SELECTED = 0xffd166;
export const COLOR_ACCENT = 0x55dde0;
export const COLOR_CAPTURING = 0xff6b6b;
